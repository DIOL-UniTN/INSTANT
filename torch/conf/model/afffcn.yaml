_target_: models.AdvancedFFFComplexNodes
_partial_: True # in_channels, num_classes (out_dim), image_size (in_hxw[0] or in_hxw) / before

leaf_width: 16
depth: 4
channel_rate: 1
complexity_order: 1 # 0 (incresing) or 1 (decreasing) [root to leaves]
